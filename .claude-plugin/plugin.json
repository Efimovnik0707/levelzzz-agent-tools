{
  "name": "ascend",
  "version": "0.2.0",
  "description": "MCP-инструменты Ascend: список задач, отметка выполнения, добавление задач и прогресс прямо из чата с агентом.",
  "repository": "https://github.com/Efimovnik0707/ascend-agent-tools",
  "skills": ["./skills"],
  "userConfig": {
    "api_key": {
      "type": "string",
      "title": "Ascend API key",
      "description": "Персональный ключ из профиля Ascend: Профиль → Агентский доступ → Создать",
      "sensitive": true,
      "required": true
    }
  },
  "mcpServers": {
    "ascend": {
      "command": "npx",
      "args": [
        "-y",
        "github:Efimovnik0707/ascend-agent-tools"
      ],
      "env": {
        "ASCEND_API_KEY": "${user_config.api_key}"
      }
    }
  }
}
